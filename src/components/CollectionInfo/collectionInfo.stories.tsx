import { Story, Meta } from '@storybook/react'
import CollectionInfo from '.'
export default {
  title: 'CollectionInfo',
  component: CollectionInfo
} as Meta

export const Default: Story = () => <CollectionInfo />
