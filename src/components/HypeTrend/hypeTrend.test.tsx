import '@testing-library/react'

describe('<HypeTrend />', () => {
  it('should render the heading', () => {
    // const { container } = render(<HypeTrend />)
    // expect(container.firstChild).toMatchSnapshot()
  })
})
